"""HTTP 路由层：按业务标签把原来的 54 个接口拆成六个 APIRouter。

拆分依据是接口上早就写好的中文分组标签，业务边界是现成的：

===============  ==========================  ============================================
模块             标签                        职责
===============  ==========================  ============================================
``system.py``    ``基础``                    连通性/健康探测、脱敏配置、统计、数据源、首页与 favicon
``papers.py``    ``文献库``                  文献列表/详情/删除、本地混合检索、题录导入、参考文献、全文、速读
``search.py``    ``检索`` + ``问答``         检索式预览、联网检索（Scout）、知识库问答（SSE）
``agent.py``     ``Agent`` + ``产物``        研究工作流的启动/SSE/审批/取消/续跑/历史 + 产物读写
``library.py``   ``会话``/``课题``/``维护``   会话与消息、课题管理、嵌入/全文补齐/FTS/VACUUM 维护
``writing.py``   ``引用``/``论文写作``/``学习闭环``  引用格式化与导出、论文初稿、用户反馈与纠错记忆
===============  ==========================  ============================================

两条纪律（避免把 god function 换成 god package）：

1. router 之间不得互相 import；确有共享就上提到 ``medscholar/server/deps.py``。
2. 这里只做"注册路由 + 调业务模块"，不含业务逻辑；应用的装配仍归 ``app.py``。
"""

from __future__ import annotations
